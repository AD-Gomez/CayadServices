<div class="visual-flow-container pointer-events-none absolute inset-0 z-40 overflow-visible">
    <svg 
      class="w-full h-full" 
      viewBox="0 0 1440 900" 
      preserveAspectRatio="xMidYMid slice"
      xmlns="http://www.w3.org/2000/svg"
    >
      <defs>
        <marker id="arrowhead" markerWidth="8" markerHeight="8" 
        refX="7" refY="4" orient="auto">
          <path d="M0,0 L8,4 L0,8 Z" fill="white" />
        </marker>
      </defs>
      
      <path 
        d="M 180,500 
           C 300,500 450,280 650,480 
           S 850,720 940,660" 
        fill="none" 
        stroke="white" 
        stroke-width="5" 
        stroke-linecap="round"
        marker-end="url(#arrowhead)"
        class="flow-line"
      />
    </svg>
</div>
  
<style>
    /* Oculto en mobile, visible en desktop */
    .visual-flow-container {
        display: none;
    }

    @media (min-width: 1024px) {
        .visual-flow-container {
            display: block;
            opacity: 0.8;
        }
    }

    .flow-line {
      /* Sombra más discreta para acompañar la línea fina */
      filter: drop-shadow(2px 3px 5px rgba(0,0,0,0.3));
      
      /* Animación de dibujado al cargar */
      stroke-dasharray: 2000;
      stroke-dashoffset: 2000;
      animation: draw-line 2.5s ease-out forwards;
    }

    @keyframes draw-line {
      to {
        stroke-dashoffset: 0;
      }
    }
</style>